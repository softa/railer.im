.content_wrapper
  .container
    .wrapper
      .list_with_pictures
        %h1= @city
        %ul
          - for user in @users
            %li
              = link_to profile_path(user) do
                #{gravatar user.gravatar_id, 40}
                #{user.name}